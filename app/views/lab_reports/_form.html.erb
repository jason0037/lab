<%= form_for @lab_report, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :desc, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :desc, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :parent_report_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :parent_report_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :child_report_index, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :child_report_index, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                lab_reports_path, :class => 'btn' %>
  </div>
<% end %>

<%= form_for @option, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group" style="display:none">
    <%= f.label :key, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :key,:value=>'course_type', :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :index,'位置', :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :index, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :name,'名称', :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :value,'值', :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :value, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit '确定', :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                options_path, :class => 'btn' %>
  </div>
<% end %>

<%= form_for @lab_scene, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name,'名称', :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :limit,'播放次数', :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :limit, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :desc,'描述', :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :desc, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :secens_resource_id,'多媒体资源', :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :secens_resource_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit '确定', :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                lab_scenes_path, :class => 'btn' %>
  </div>
<% end %>

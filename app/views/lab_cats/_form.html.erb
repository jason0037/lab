<%= form_for @lab_cat, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :parent_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :parent_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :cat_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :cat_name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :disabled, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :disabled, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                lab_cats_path, :class => 'btn' %>
  </div>
<% end %>

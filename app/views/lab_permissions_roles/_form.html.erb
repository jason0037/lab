<%= form_for @lab_permissions_role, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :role_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :role_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :permission_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :permission_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                lab_permissions_roles_path, :class => 'btn' %>
  </div>
<% end %>
